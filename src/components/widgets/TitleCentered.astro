---
export interface Props {
  data?: {
    title?: string;
    subtitle?: string;
    isSticky?: boolean;
  };
}
const { data = {} } = Astro.props;
---

<div class={`flex flex-col ${data.isSticky ? 'sticky' : ''} top-20 pb-16`}>
  {
    data.subtitle && (
      <div
        class="badge animate-on-scroll"
        data-type="simple"
        data-delay="0"
        data-duration="2.5"
        data-easing="Expo.easeOut"
      >
        <span class="badge-text ">{data.subtitle}</span>
      </div>
    )
  }
  {
    data.title && (
      <h2
        class="headingTxt text-center text-[3.5rem] leading-[4rem] animate-on-scroll"
        data-type="simple"
        data-delay="0.15"
        data-duration="2.5"
        data-easing="Expo.easeOut"
        set:html={data.title}
      />
    )
  }
</div>
